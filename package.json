{
  "name": "custom-dink-webhook",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.7",
    "wrangler": "^3.88.0"
  }
}
